{
  "name": "bettercodex-plugins",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Community registry of plugins, themes, and skills for BetterCodex.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/build-catalog.mjs",
    "check": "node scripts/validate.mjs && node scripts/build-catalog.mjs --check"
  },
  "engines": {
    "node": ">=18"
  }
}
